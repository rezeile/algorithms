/*
 * A program that compares various sorting algorithms 
 */

 #include <ctime>
 #include <string>
 #include "merge-sort/merge.hh"
 #include "insertion-sort/insertion.hh"
 #include "selection-sort/selection.hh"
 #include "shell-sort/shell.hh"

 
 int main(int argc, char *argv[]) {
    std::string s1(argv[1]);
    std::string s2(argv[2]);
    std::string num(argv[3]);
    int n = std::stoi(num);
 	return 0;
 }